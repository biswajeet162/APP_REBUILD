# project/

Drop the **decompiled or extracted** Android app in this folder (apktool output, jadx project, Flutter/RN extract, etc.).

Then tell the agent to analyze or rebuild. Stage 1 reads **this folder only** as the reference dump.

Do not put the new app here. The rebuilt app is created in `../new-project/`.
